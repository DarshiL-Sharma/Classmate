import 'package:flutter/material.dart';

class AppColors {
  static const Color dotInactive = Color(0xFFE0E0E0);
  static const Color background = Color(0xFFFFFFFF); // pure white app bg
  static const Color cardBeige = Color(0xFFFFFFFF); // soft off-white card surface
  static const Color cardBeigeLight = Color(0xFFFDF2F4); // Very light pinkish tint for variety
  static const Color accentGreen = Color(0xFFFF5E7D); // Your new Hex: #FF5E7D
  static const Color accentGreenDark = Color(0xFFD64A65); // Darker shadow for 3D effect
  static const Color accentPurple = Color(0xFF2D2D2D); // Deep charcoal instead of pure black
  static const Color textDark = Color(0xFF111111); // near-black text
  static const Color textGrey = Color(0xFF6B6B6B); // muted grey description text
  static const Color white = Color(0xFFFFFFFF);
}
